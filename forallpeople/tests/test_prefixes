import forallpeople as si
si.environment('structural_copy', top_level=True)

g = 9.81 * m/s**2
m = 3500000 * kg
force = m * g
print(force)

# Ideas:
# Add Default to the structural.json file as needed
# Add a check in _evaluate_dims_and_factor for our default units to set prefix_bool = False